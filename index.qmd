---
pagetitle: "Peekbank"
---

{{< include components/_hero.qmd >}}

```{ojs stats}
//| output: false
manifest = d3.json((typeof window !== "undefined" && window.PEEKBANK_DATA_BASE || "slices/") + "manifest.json")
totals = ({
  datasets: manifest.datasets.length,
  subjects: d3.sum(manifest.datasets, (d) => d.n_subjects),
})
```

```{=html}
<div class="pb-headline">
  Peekbank is an open database of infant looking-while-listening data
</div>
```

<div style="text-align:center; margin: 1.6rem 0 2.6rem 0;">
<a class="pb-cta" href="accuracy.html">Get Started →</a>&nbsp;&nbsp;
<a class="pb-cta" style="background:#e5e7eb; color:#111827 !important;" href="dataaccess.html">Get the data</a>
</div>

![](images/peekbank_item_vis.png){width=100% fig-alt="Proportion target looking curves for each item across fifteen Peekbank datasets"}

# What is peekbank? {.pb-serif}

```{ojs what-is}
html`<p>The <b>peekbank</b> project aims to democratize access to
developmental eye-tracking datasets. The project (1) collects a large set
of eye-tracking datasets using the looking-while-listening paradigm —
currently <b>${totals.datasets} datasets</b> with a total of
<b>${totals.subjects.toLocaleString()} children</b> tested — (2) introduces
a common format and processing tools for such data, and (3) provides
interactive tools and an API for accessing and analyzing the data.</p>`
```

You can [explore the data](accuracy.html) right in your browser, read the
[documentation](docs.html), [access the full database](dataaccess.html)
via R, Python, or SQL, and [browse every dataset](datasets.html). To
contribute a dataset, contact
[peekbank-dev@lists.stanford.edu](mailto:peekbank-dev@lists.stanford.edu).

<div style="text-align:center; margin: 1.6rem 0 1rem 0;">
<a class="pb-cta" href="accuracy.html">Get Started →</a>
</div>
